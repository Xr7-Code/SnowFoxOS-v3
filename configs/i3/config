# ============================================================
#   SnowFoxOS v3 — i3 Konfiguration (SnowFox Palette)
# ============================================================

set $mod Mod4
set $term kitty

# ── Farben ──────────────────────────────────────────────────
set $purple    #9d6fe8
set $purple_lt #b899ff
set $bg        #1a1825
set $bg2       #201e2e
set $fg        #e0d9f5
set $gray      #6e6a8a
set $orange    #ff79c6

# ── Schrift ─────────────────────────────────────────────────
font pango:JetBrainsMono Nerd Font 10

# ── Fenster-Dekoration ──────────────────────────────────────
# Tiling: kein Rahmen, kein Titel — clean
# Floating: schmale Titelleiste zum Anfassen + lila Rahmen
client.focused          $purple   $purple   $fg     $purple_lt $purple
client.focused_inactive $bg2      $bg       $gray   $bg2       $bg2
client.unfocused        $bg2      $bg       $gray   $bg2       $bg2
client.urgent           $orange   $bg2      $fg     $orange    $orange

default_border none
default_floating_border normal 2

# Fenster-Regeln beim Start
for_window [tiling] border none
for_window [floating] border normal 2

# ── Gaps ────────────────────────────────────────────────────
gaps inner 8
gaps outer 4
smart_gaps on
smart_borders on

# ── Autostart ───────────────────────────────────────────────
exec_always --no-startup-id ~/.fehbg
exec --no-startup-id picom --config ~/.config/picom.conf
exec --no-startup-id ~/.config/polybar/launch.sh
exec --no-startup-id dunst
exec --no-startup-id lxpolkit
exec --no-startup-id xsettingsd
exec --no-startup-id nm-applet --no-agent
exec --no-startup-id redshift
exec --no-startup-id greenclip daemon
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork -c 1e1e2e
exec_always --no-startup-id dbus-update-activation-environment --systemd QT_QPA_PLATFORMTHEME

# ── Shortcuts ───────────────────────────────────────────────
bindsym $mod+Return exec $term
bindsym $mod+space  exec rofi -show drun -show-icons -theme ~/.config/rofi/config.rasi
bindsym $mod+Tab    exec rofi -show window -theme ~/.config/rofi/config.rasi
bindsym $mod+c      exec rofi -modi "clipboard:greenclip print" -show clipboard \
                         -run-command '{cmd}' -theme ~/.config/rofi/config.rasi
bindsym $mod+e      exec pcmanfm
bindsym $mod+n      exec ~/.config/snowfox-network.sh
bindsym $mod+w      exec ~/.config/snowfox-wallpaper.sh
bindsym $mod+l      exec i3lock -c 1e1e2e
bindsym $mod+p      exec ~/.config/snowfox-display.sh

# ── Screenshots ─────────────────────────────────────────────
bindsym Print        exec scrot ~/Pictures/screenshot_%Y%m%d_%H%M%S.png \
                          -e 'xclip -selection clipboard -t image/png -i $f' \
                          && notify-send "SnowFox" "Screenshot gespeichert"
bindsym $mod+Print  exec scrot -s ~/Pictures/screenshot_%Y%m%d_%H%M%S.png \
                          -e 'xclip -selection clipboard -t image/png -i $f' \
                          && notify-send "SnowFox" "Ausschnitt gespeichert"

# ── Audio ────────────────────────────────────────────────────
bindsym XF86AudioRaiseVolume exec wpctl set-volume -l 1.0 @DEFAULT_AUDIO_SINK@ 5%+
bindsym XF86AudioLowerVolume exec wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindsym XF86AudioMute        exec wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle

# ── Helligkeit ───────────────────────────────────────────────
bindsym XF86MonBrightnessUp   exec brightnessctl set +5%
bindsym XF86MonBrightnessDown exec brightnessctl set 5%-

# ── Medien ───────────────────────────────────────────────────
bindsym XF86AudioPlay  exec playerctl play-pause
bindsym XF86AudioNext  exec playerctl next
bindsym XF86AudioPrev  exec playerctl previous

# ── System ───────────────────────────────────────────────────
bindsym $mod+q        kill
bindsym $mod+Shift+r  reload
bindsym $mod+Shift+e  exec ~/.config/powermenu.sh
bindsym XF86PowerOff  exec --no-startup-id ~/.config/powermenu.sh

# ── Fensterverwaltung ────────────────────────────────────────
bindsym $mod+Left  focus left
bindsym $mod+Right focus right
bindsym $mod+Up    focus up
bindsym $mod+Down  focus down

bindsym $mod+Shift+Left  move left
bindsym $mod+Shift+Right move right
bindsym $mod+Shift+Up    move up
bindsym $mod+Shift+Down  move down

bindsym $mod+h split h
bindsym $mod+v split v
bindsym $mod+f fullscreen toggle
bindsym $mod+s layout stacking
bindsym $mod+t layout toggle split

# Floating Toggle mit automatischer Rahmen- & Größenanpassung
bindsym $mod+Shift+space floating toggle; [con_id="__focused__" floating] border normal 2, resize set 960 600, move position center; [con_id="__focused__" tiling] border none

bindsym $mod+d           focus mode_toggle

# Floating: Maus-Drag zum Verschieben (Titelleiste) + Mod+Klick als Fallback
floating_modifier $mod

# ── Workspaces ───────────────────────────────────────────────
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"

bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9

bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9

# ── Floating Regeln ──────────────────────────────────────────
for_window [class="Pavucontrol"]           floating enable, resize set 640 440
for_window [class="Nm-connection-editor"]  floating enable
for_window [class="System-config-printer"] floating enable
for_window [window_role="dialog"]          floating enable
for_window [window_role="pop-up"]          floating enable
for_window [class="jetbrains-toolbox"]     floating enable, move position center
for_window [class="MEGAsync"]              floating enable, move position center
for_window [title="MEGAsync"]              floating enable, move position center
no_focus [class="MEGAsync"]

# ── Resize Mode ──────────────────────────────────────────────
mode "resize" {
    bindsym Left  resize shrink width  20px
    bindsym Right resize grow   width  20px
    bindsym Up    resize shrink height 20px
    bindsym Down  resize grow   height 20px
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

# Polybar — kein i3bar
bar {
    i3bar_command :
}
